=== Job description ===

This Job will start an HTTP2 server in order to generate traffic. It launchs the nghttpd command (without TLS).

=== Example ===

Launch a server listening on port ''80''.

In the web interface, set parameters as follows:
  * **port** = 80

Or launch the job manually from CLI on the agent as follows:
<code>
JOB_NAME=http2_server sudo -E python3 /opt/openbach/agent/jobs/http2_server/http2_server.py 80
</code>